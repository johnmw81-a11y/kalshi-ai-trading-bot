- name: Run Trading Script
        env:
          KALSHI_KEY_ID: ${{ secrets.KALSHI_KEY_ID }}
          KALSHI_API_KEY: ${{ secrets.KALSHI_KEY_ID }}
          KALSHI_PRIVATE_KEY: ${{ secrets.KALSHI_PRIVATE_KEY }}
          KALSHI_API_BASE_64_KEY: ${{ secrets.KALSHI_PRIVATE_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          AI_PROVIDER: "google"
          MODEL_NAME: "gemini-2.0-flash"
          MARKET_TOPIC: "NCAAM, MLB"
          # SPEED BOOST: Only process 5 markets and skip slow news searches
          MAX_MARKETS_TO_PROCESS: "5"
          ENABLE_NEWS_SEARCH: "false" 
          # RISK MANAGEMENT
          MAX_ORDER_VALUE: "20"
          STAKE_USD: "20"
          MIN_CONFIDENCE_THRESHOLD: "0.60"
          LOG_LEVEL: "DEBUG"
          XAI_API_KEY: "not-used"
          OPENAI_API_KEY: "not-used"
          OPENROUTER_API_KEY: "not-used"
        run: python beast_mode_bot.py
